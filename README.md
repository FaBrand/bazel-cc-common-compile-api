# Build

```
USE_BAZEL_VERSION=6.2.1 bazel build with_label_name
USE_BAZEL_VERSION=6.2.1 bazel build with_other_name

USE_BAZEL_VERSION=7.2.1 bazel build with_label_name
USE_BAZEL_VERSION=7.2.1 bazel build with_other_name
```
